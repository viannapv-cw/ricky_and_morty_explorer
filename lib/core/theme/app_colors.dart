import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const primary = Color(0xFF97CE4C);     // Verde Rick
  static const secondary = Color(0xFF4ECFED);   // Azul portal
  static const portalGreen = Color(0xFF88E97D); // Verde brilhante do portal
  
  // Cores do tema escuro
  static const darkBackground = Color(0xFF202428);
  static const darkSurface = Color(0xFF303438);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB5B5B5);
  
  // Cores do tema claro
  static const lightBackground = Color(0xFFF5F5F5);  // Cinza muito claro
  static const lightSurface = Color(0xFFFFFFFF);     // Branco
  static const lightTextPrimary = Color(0xFF2C3E50); // Cinza escuro para texto
  static const lightTextSecondary = Color(0xFF7F8C8D); // Cinza médio para texto secundário
  
  // Cores de status
  static const statusAlive = Color(0xFF97CE4C);
  static const statusDead = Color(0xFFE84855);
  static const statusUnknown = Color(0xFF9E9E9E);
  
  // Cores de ação
  static const favorite = Color(0xFFE84855);
  static const error = Color(0xFFE84855);
} 